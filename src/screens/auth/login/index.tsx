import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const Login = () => {
  return (
    <SafeAreaView>
      <Text>Login</Text>
    </SafeAreaView>
  );
};

export default Login;
